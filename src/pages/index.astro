---
// Custom landing page with PostHog analytics
import PostHogLayout from '../layouts/PostHogLayout.astro';
---
<PostHogLayout
  title="AI Workshop"
  description="Learn to build with Claude Code - from complete beginner to deployed project"
>
  <link slot="head" rel="preconnect" href="https://fonts.googleapis.com">
  <link slot="head" rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link slot="head" href="https://fonts.googleapis.com/css2?family=Instrument+Serif&family=Inter:wght@400;500&display=swap" rel="stylesheet">
  <style slot="head">
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      height: 100%;
    }

    body {
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: #ffffff;
      color: #1a1a1a;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 2rem;
      text-align: center;
    }

    .container {
      max-width: 600px;
    }

    h1 {
      font-family: 'Instrument Serif', Georgia, serif;
      font-size: clamp(2.5rem, 8vw, 3.5rem);
      font-weight: 400;
      line-height: 1.15;
      margin-bottom: 1.5rem;
      letter-spacing: -0.02em;
    }

    p {
      font-size: 1.125rem;
      color: #555;
      line-height: 1.6;
      margin-bottom: 2.5rem;
    }

    .cta {
      display: inline-block;
      padding: 1rem 2.5rem;
      font-size: 1rem;
      font-weight: 500;
      font-family: inherit;
      color: white;
      background: #2563eb;
      border: none;
      border-radius: 50px;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.2s ease;
      box-shadow: 0 4px 14px rgba(37, 99, 235, 0.3);
    }

    .cta:hover {
      background: #1d4ed8;
      transform: translateY(-1px);
      box-shadow: 0 6px 20px rgba(37, 99, 235, 0.35);
    }

    @media (max-width: 480px) {
      body {
        padding: 1.5rem;
      }

      p {
        font-size: 1rem;
      }

      .cta {
        padding: 0.875rem 2rem;
      }
    }
  </style>

  <div class="container">
    <h1>Learn to build with AI</h1>
    <p>
      From complete beginner to deployed project in 3 hours.<br>
      No coding experience required.
    </p>
    <a href="/ai-workshop/introduction/" class="cta">Start Learning</a>
  </div>
</PostHogLayout>
